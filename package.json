{
  "name": "udev-generator",
  "version": "1.0.1",
  "packageManager": "yarn@4.10.3",
  "main": "dist/main.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Julusian/node-udev-generator.git"
  },
  "scripts": {
    "build": "rimraf dist && run build:main",
    "build:main": "tsc --project tsconfig.build.json",
    "lint": "eslint . --ext .ts",
    "prettier": "prettier --write .",
    "watch-types": "tsc --watch --project tsconfig.json",
    "test": "vitest"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@tsconfig/node20": "^20.1.6",
    "@types/node": "^20.19.17",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
